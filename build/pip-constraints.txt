# mediasoup-sys invokes pip from its Cargo build script. Constrain every package
# that script installs so a later PyPI release cannot silently enter the build.
invoke==3.0.3
pip==26.2.1
setuptools==84.0.0
# Keep these exact versions aligned with the maintained worker tasks.py pins.
meson==1.12.0
ninja==1.13.2
